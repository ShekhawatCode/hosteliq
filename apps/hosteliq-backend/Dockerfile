# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build hosteliq-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t hosteliq-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system hosteliq-backend && \
          adduser --system -G hosteliq-backend hosteliq-backend

COPY dist/apps/hosteliq-backend hosteliq-backend
RUN chown -R hosteliq-backend:hosteliq-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix hosteliq-backend --omit=dev -f install

CMD [ "node", "hosteliq-backend" ]
